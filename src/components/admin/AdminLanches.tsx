import AdminCompositeProducts from "./AdminCompositeProducts";

export default function AdminLanches() {
  return (
    <AdminCompositeProducts
      type="lanche"
      title="Lanches"
      description="Gerencie lanches compostos por ingredientes do estoque"
    />
  );
}
